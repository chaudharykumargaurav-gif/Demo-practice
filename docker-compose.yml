version: "3.8"

services:
  minimal-springboot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minimal-springboot-app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped

